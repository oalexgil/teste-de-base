{
  "name": "tom-de-base",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based colorimetry and face-tracking prototype for cosmetic shade matching.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --test tests/*.test.js",
    "ci": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
