# Naming and repository note

The product name used throughout this branch is **AdSignal Intelligence**.

The current GitHub repository is still named `teste-de-base` because the available repository connector in this session can create branches, files and pull requests but does not expose repository creation or rename actions.

Before public launch, rename the repository to a professional product slug such as:

- `adsignal-intelligence`
- `adsignal-ai`
- `adsignal-performance`

The code, documentation and branch structure are already product-branded and can move without architecture changes.
